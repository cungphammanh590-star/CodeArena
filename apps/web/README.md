# CodeArena Web

Vue 3 + Vite + TypeScript frontend for CodeArena.

## Develop

```bash
npm install
npm run dev
```

Dev server proxies `/api`, `/health`, and `/submit` to `http://localhost:8080`.

## Build

```bash
npm run build
```
